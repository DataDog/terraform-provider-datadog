---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "datadog_team_connection Resource - terraform-provider-datadog"
subcategory: ""
description: |-
  Provides a Datadog Team Connection resource. This can be used to create and manage connections between a Datadog team and an external team (e.g. GitHub).
---

# datadog_team_connection (Resource)

Provides a Datadog Team Connection resource. This can be used to create and manage connections between a Datadog team and an external team (e.g. GitHub).

## Example Usage

```terraform
resource "datadog_team" "example" {
  description = "Example team"
  handle      = "example-team"
  name        = "Example Team"
}

# Create a connection between a Datadog team and a GitHub team
resource "datadog_team_connection" "example" {
  team {
    id   = datadog_team.example.id
    type = "team"
  }
  connected_team {
    id   = "@GitHubOrg/team-handle"
    type = "github_team"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `connected_team` (Block, Optional) The external connected team reference (e.g. a GitHub team). (see [below for nested schema](#nestedblock--connected_team))
- `source` (String) The source of the connection (e.g. github).
- `team` (Block, Optional) The Datadog team reference. (see [below for nested schema](#nestedblock--team))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--connected_team"></a>
### Nested Schema for `connected_team`

Required:

- `id` (String) The ID of the external connected team.
- `type` (String) The resource type of the external connected team. Valid values are `github_team`.


<a id="nestedblock--team"></a>
### Nested Schema for `team`

Required:

- `id` (String) The ID of the Datadog team.
- `type` (String) The resource type of the Datadog team. Valid values are `team`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import datadog_team_connection.example "${connection_id}"
```
