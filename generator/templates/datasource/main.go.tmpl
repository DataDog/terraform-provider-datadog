{{- define "main.go.tmpl" -}}
// Code generated by dd-tf-generator; DO NOT EDIT.

package {{ .PackageName }}

import (
	"context"

	"github.com/DataDog/datadog-api-client-go/v2/api/{{ .SDKImportPath }}"
	"github.com/hashicorp/terraform-plugin-framework/datasource"
	"github.com/hashicorp/terraform-plugin-framework/datasource/schema"
	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/types"
{{- if .HasListFallback }}
	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework-validators/datasourcevalidator"
{{- end }}

	"github.com/terraform-providers/terraform-provider-datadog/datadog/internal/utils"
)

var _ datasource.DataSource = &datadog{{ .TypeName }}DataSource{}
{{- if .HasListFallback }}
var _ datasource.DataSourceWithConfigValidators = &datadog{{ .TypeName }}DataSource{}
{{- end }}

// {{ .TypeName }}DataSourceHooksInterface defines extension points for the {{ snakeCase .TypeName }} data source.
type {{ .TypeName }}DataSourceHooksInterface interface {
	BeforeRead(context.Context, datasource.ReadRequest, interface{}) diag.Diagnostics
	AfterRead(context.Context, interface{}) diag.Diagnostics
	ModifySchema(*schema.Schema)
}

func NewDatadog{{ .TypeName }}DataSource() datasource.DataSource {
	return &datadog{{ .TypeName }}DataSource{
		Hooks: &{{ .TypeName }}DataSourceHooks{},
	}
}

type datadog{{ .TypeName }}DataSource struct {
	Api   *{{ .SDKImportPath }}.{{ .SDKApiType }}
	Auth  context.Context
	Hooks {{ .TypeName }}DataSourceHooksInterface
}

{{ template "model" . }}

func (d *datadog{{ .TypeName }}DataSource) Configure(_ context.Context, request datasource.ConfigureRequest, response *datasource.ConfigureResponse) {
	providerData, _ := request.ProviderData.(*FrameworkProvider)
	d.Api = providerData.DatadogApiInstances.{{ .SDKApiAccessor }}()
	d.Auth = providerData.Auth
}

func (d *datadog{{ .TypeName }}DataSource) Metadata(_ context.Context, req datasource.MetadataRequest, resp *datasource.MetadataResponse) {
	resp.TypeName = "{{ snakeCase .TypeName }}"
}

{{ template "schema" . }}

{{ template "read" . }}

{{ template "state" . }}
{{- if .HasListFallback }}

{{ template "validators" . }}
{{- end }}
{{ end -}}
