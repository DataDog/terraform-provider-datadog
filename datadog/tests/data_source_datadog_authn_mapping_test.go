// Generated by tfgen as an acceptance-test scaffold. Complete the TODOs and
// record a cassette (see below); tfgen will not overwrite this file once it exists.
package test

import (
	"context"
	"fmt"
	"strings"
	"testing"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"
)

// TestAccDatadogAuthnMappingDatasource is a generated scaffold. It needs a recorded cassette to pass
// in replay mode:
//  1. Complete testAccDatasourceDatadogAuthnMappingConfig below: add the resource(s) that seed the data
//     this data source reads, and set the lookup/filter value(s).
//  2. Record the cassette: RECORD=true make testacc TESTARGS='-run TestAccDatadogAuthnMappingDatasource'
//  3. Review datadog/tests/cassettes/TestAccDatadogAuthnMappingDatasource.yaml (and its .freeze), then commit them so the test
//     runs in replay mode.
func TestAccDatadogAuthnMappingDatasource(t *testing.T) {
	t.Parallel()
	ctx, _, accProviders := testAccFrameworkMuxProviders(context.Background(), t)
	uniq := strings.ToLower(uniqueEntityName(ctx, t))

	resource.Test(t, resource.TestCase{
		PreCheck:                 func() { testAccPreCheck(t) },
		ProtoV6ProviderFactories: accProviders,
		Steps: []resource.TestStep{
			{
				Config: testAccDatasourceDatadogAuthnMappingConfig(uniq),
				Check: resource.ComposeTestCheckFunc(
					resource.TestCheckResourceAttrSet("data.datadog_authn_mapping.foo", "id"),
					// TODO(tfgen): assert specific attributes once the cassette is recorded.
				),
			},
		},
	})
}

func testAccDatasourceDatadogAuthnMappingConfig(uniq string) string {
	return fmt.Sprintf(`
# TODO(tfgen): create the resource this data source reads (use %[1]q for unique names), then reference it below.
data "datadog_authn_mapping" "foo" {
	filter = %[1]q
	page_number = 0
	page_size = 0
}
`, uniq)
}
