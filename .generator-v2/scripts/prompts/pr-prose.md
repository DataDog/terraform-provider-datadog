You are writing the "How to test" section of a pull request that adds a Terraform data
source generated by tfgen. Write clear, copy-pasteable steps a human reviewer will follow.

Key facts, always true here:
- The generated acceptance test ships as an unrecorded scaffold. A human records it later;
  you are not recording anything and must not claim it is verified.
- Tests run in two modes. Record mode hits the live Datadog Frog org and writes an HTTP
  cassette. Replay mode runs the same test offline against that cassette — this is the gate
  CI enforces.
- Credentials come from the Frog test org. Pull them with dd-auth and map them to the env
  vars the recorder reads. Do NOT set a site URL — the keys route themselves.

You are given, after this prompt: the artifact name, the cardinality, and the acceptance
test function name and file.

## Steps to cover, in order

1. Get Frog test-org credentials and map them:
   - `eval "$(dd-auth --domain frog.datadoghq.com --force-app-key --no-cache --output)"`
   - export `DD_TEST_CLIENT_API_KEY` from `DD_API_KEY` and `DD_TEST_CLIENT_APP_KEY` from
     `DD_APP_KEY`; do not set `DD_TEST_SITE_URL`.
2. Record (writes/updates the cassette), from the repo root, using the given test function:
   `make testacc RECORD=true TESTARGS='-run <TestFunc>'`
3. Commit the cassette AND its `.freeze` companion from `datadog/tests/cassettes/`, named
   exactly after the test (`<TestFunc>.yaml` and `<TestFunc>.freeze`).
4. Replay — this is what CI runs and the only thing that proves it works:
   `make testacc RECORD=false TESTARGS='-run <TestFunc>'`
   Only after a green replay may anyone call the data source verified.

## If cardinality is plural, also include a short caveat

The generated test should depend on its seed resource so the seed is created before the
list read, and it should assert with set-membership (a `TestCheckTypeSetElemNestedAttrs`
against `<collection>.*`), never a fixed index or a hard-coded count — the org holds other
records in unstable order. Tell the reviewer to confirm the generated test does this.

## Output

Return STRICT JSON and nothing else — no prose before or after, no markdown code fences:

{"how_to_test": "<the section body as GitHub-flavored markdown; use fenced bash blocks for commands and substitute the real test function name>"}

Do not include a heading like "## How to test" in the value — only the body. Do not add any
disclaimer about generated code; the PR already carries that separately.
